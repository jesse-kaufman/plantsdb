/**
 * @file Constants used throughout the Plant class.
 */

/** List of valid stages. */
export const validStages = ["seedling", "veg", "flower", "harvested", "cure"]

/** List of valid plant statuses. */
export const validStatuses = ["active", "inactive", "archived"]
/** Number of estimated weeks in seedling stages. */
export const seedlingWeeks = 1
/** Number of estimated weeks in veg stages. */
export const vegWeeks = 4
/** Number of estimated weeks in flower. */
export const flowerWeeks = 8
